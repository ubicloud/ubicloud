! Invalid ssh public key reference ("spk/"), should not include /

Manage SSH public keys

Usage:
    ubi sk command [...]
    ubi sk (sk-name | sk-id) post-command [...]

Commands:
    list       List SSH public keys

Post Commands:
    create     Register an SSH public key
    destroy    Destroy an SSH public key
    rename     Rename an SSH public key
    show       Show details for an SSH public key
    update     Update an SSH public key
