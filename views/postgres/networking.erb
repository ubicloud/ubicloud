<div class="section-padded">
  <div class="flex-header">
    <div class="flex-title">
      <h3>Firewall Rules</h3>
      <p class="text-muted-spaced">
      <% if firewall = @pg.customer_firewall %>
          You can 
          <a class="link-primary" href="<%= path(firewall) %>/networking">manage firewall rules using the firewall for this PostgreSQL database</a>.
      <% else %>
          The firewall related to this PostgreSQL database was deleted or detached from the related private subnet,
          you can manage firewall rules using an appropriate firewall on the
          <a class="link-primary" href="<%= path(@pg.private_subnet) %>/networking">private subnet</a>.
      <% end %>
      </p>
    </div>
  </div>
</div>
