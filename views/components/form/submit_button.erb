<%# locals: (text:, name: nil, attributes: {}, extra_class: nil) %>

<%== render(
  "components/button",
  locals: {
    text:,
    extra_class:,
    attributes: { "type" => "submit", "name" => name }.merge!(attributes)
  }
) %>
