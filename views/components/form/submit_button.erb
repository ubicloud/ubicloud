<% name ||= nil
extra_class ||= nil
attributes ||= {} %>

<%== render(
  "components/button",
  locals: {
    text: text,
    extra_class: extra_class,
    attributes: { "type" => "submit", "name" => name }.merge!(attributes)
  }
) %>
