<%# locals: (title: @page_title, breadcrumbs: [], right_items: nil, back: breadcrumbs.dig(-2, 1) || "#") %>

<div class="page-header">
  <% unless breadcrumbs.empty? %>
    <div>
      <nav class="sm:hidden" aria-label="Back">
        <a href="<%= back %>" class="flex-center breadcrumb-link no-underline">
          <%== part("components/icon", name: "hero-chevron-left", classes: "icon-sm shrink-0 text-gray-400 -ml-1 mr-1") %>
          Back
        </a>
      </nav>
      <nav class="breadcrumb hidden sm:flex" aria-label="Breadcrumb">
        <ol role="list" class="flex-center gap-4">
          <% breadcrumbs.each_with_index do |(name, url), index| %>
            <li>
              <div class="flex-center">
                <% if index > 0 %>
                  <span class="mr-4">
                    <%== part("components/icon", name: "hero-chevron-right", classes: "icon-sm shrink-0 text-gray-400") %>
                  </span>
                <% end %>
                <a href="<%= url %>" class="breadcrumb-link"><%= name %></a>
              </div>
            </li>
          <% end %>
        </ol>
      </nav>
    </div>
  <% end %>

  <div class="flex-header pb-4">
    <div class="min-w-0">
      <h2 class="page-heading-lg"><%= title %></h2>
    </div>
    <% if right_items %>
      <div class="action-buttons">
        <%== right_items.join %>
      </div>
    <% end %>
  </div>
</div>
