<%# locals: (text: "Download", link:) %>

<%== render(
  "components/button",
  locals: {
    link: link,
    text: text,
    icon: "hero-document-arrow-down",
    extra_class: "download-btn",
    type: "primary"
  }
) %>
